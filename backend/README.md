# Cockpit APT Bridge

Python backend for cockpit-apt package manager.

## Usage

This package provides a command-line interface for APT operations that can be called by the Cockpit frontend via `cockpit.spawn`.

See the main [README.md](../README.md) in the project root for full documentation.
